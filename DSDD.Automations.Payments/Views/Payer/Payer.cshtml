@model DSDD.Automations.Payments.Views.Payer.PayerViewModel

@{
    Layout = "../Shared/_Layout.cshtml";
}

<style>
    .row-removed {
        opacity: 0.5
    }
</style>

<div class="mb-2">
    <a href="/payers" class="btn btn-secondary">Zpět</a>
    <a href="/payers/@Model.VariableSymbol/payments/new" class="btn btn-success"><i class="bi bi-plus"></i>Nová platba</a>
</div>

<table class="table table-bordered">
    <thead class="table-light">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Payments.First().Type)</th>
            <th>@Html.DisplayNameFor(model => model.Payments.First().CounterpartyAccountNumber)</th>
            <th>@Html.DisplayNameFor(model => model.Payments.First().ConstantSymbol)</th>
            <th>@Html.DisplayNameFor(model => model.Payments.First().AmountCzk)</th>
            <th>@Html.DisplayNameFor(model => model.Payments.First().DateTime)</th>
            <th>@Html.DisplayNameFor(model => model.Payments.First().Description)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var payment in Model.Payments)
        {
            @await Html.PartialAsync("_Payment.cshtml", payment);
        }
    </tbody>
</table>